# Database Configuration
# PostgreSQL connection string from Railway
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# Example: postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway?schema=public
DATABASE_URL=postgresql://localhost:5432/triage_dev

# Authentication
# Shared secret for validating inbound HubSpot webhooks
HUBSPOT_WEBHOOK_TOKEN=dev-token-change-in-production

# External Services
# Discord webhook URL for sending triage notifications
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy

# Groq API key for fallback LLM inference
GROQ_API_KEY=gsk_xxx

# Local LLM Configuration
# Cloudflare Tunnel URL for accessing local Ollama instance
LOCAL_LLM_URL=http://localhost:11434

# Authentication token for local LLM requests
# Optional: Only required if local LLM endpoint requires authentication
LOCAL_LLM_TOKEN=your-local-token-here

# Cloudflare Access credentials for authenticating to tunnel
# Not needed for local development (accessing Ollama directly)
CF_ACCESS_CLIENT_ID=not-needed-for-local
CF_ACCESS_CLIENT_SECRET=not-needed-for-local

# Runtime Configuration
# Environment: development | production
NODE_ENV=development

# Logging level: debug | info | warn | error
LOG_LEVEL=debug

# Server port (Railway sets this automatically in production)
PORT=3000
